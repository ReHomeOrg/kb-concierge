"""Исходящие webhooks событий агента (§10): transactional outbox + HMAC-доставка.

Событие пишется в outbox в ОДНОЙ транзакции с доменным изменением (NFR-8, at-least-
once); воркер доставляет подписчику с HMAC-подписью, с backoff-повтором. Полезная
нагрузка — БЕЗ ПДн (только id/enum/номера, G3).
"""
