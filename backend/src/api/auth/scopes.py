"""Именованные OAuth-скоупы kb-concierge (без магических строк в ветвлениях RBAC).

Скоуп считается бэкендом из проверенного JWT (G2: не из payload и не с фронтенда).
"""

from __future__ import annotations

STAFF_ADMIN_SCOPE = "staff_admin"
"""Админ: управление политиками автономности и конфигурацией (§10 PATCH /policies)."""

OPERATOR_SCOPE = "operator"
"""Оператор: просмотр/управление политиками, разбор эскалаций."""

AGENT_SCOPE = "agent"
"""Сервис-принципал агента-оркестратора «Консьерж».

Действия агента ОТ ИМЕНИ пользователя несут делегированную авторизацию (on-behalf-of,
G7/FR-9.7): проверки scope/access_level применяются к ПОЛЬЗОВАТЕЛЮ (actor-claim), а не
к широкому сервис-принципалу агента. SP агента — только для инфраструктурных вызовов.
"""
