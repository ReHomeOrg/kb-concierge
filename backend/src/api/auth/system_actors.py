"""Реестр системных акторов аудита kb-concierge.

`AuditLog.actor_id` — NOT NULL (инвариант: «у каждой записи аудита есть актор»).
Действия, инициированные сервисом, а не человеком, фиксируются под резервным
sentinel-UUID из диапазона `00000000-0000-0000-0000-0000000000XX`, который Keycloak
как random-v4 не выдаёт → коллизия с реальным user-id исключена.

ВАЖНО: действия агента ОТ ИМЕНИ пользователя атрибутируются ПОЛЬЗОВАТЕЛЮ
(on-behalf-of, G7), а не этим sentinel — он только для инфраструктурных действий.
"""

from __future__ import annotations

import uuid

# Сервис-принципал агента-оркестратора «Консьерж»: инфраструктурные действия
# (intent-классификация, фоновые задачи, не-on-behalf-of вызовы).
AGENT_ACTOR_ID = uuid.UUID("00000000-0000-0000-0000-0000000000c1")

# Воркер ретенции/cleanup сессий (NFR-5, 152-ФЗ right-to-forget).
RETENTION_ACTOR_ID = uuid.UUID("00000000-0000-0000-0000-0000000000a2")
